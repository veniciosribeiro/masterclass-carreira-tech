DATABASE_URL=postgres://postgres:postgres@localhost:5432/techcareer
JWT_SECRET=change-me-generate-with-openssl-rand-base64-32
PORT=4000
NODE_ENV=development

# SMTP Configuration (e.g. Mailtrap, Gmail, etc.)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@techcareer.com

# Admin Authentication (Basic Auth)
ADMIN_USER=admin
ADMIN_PASS=change-me
